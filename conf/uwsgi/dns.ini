[uwsgi]
plugins = python
workers = 1
master = true
auto-procname = true
procname-prefix-spaced = %n
module = dns.dnstest
callable = app
socket = /run/nginx/uwsgi-%n.sock
chmod-socket = 666
chdir = /srv/%n
virtualenv = /srv/%n

